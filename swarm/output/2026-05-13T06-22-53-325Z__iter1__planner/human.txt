Queued `006-context-window-cache-ttl.pending.md`.

Rationale:
- Queue 001-005 cover G1 (cost) + G2 (rem) + G3 items #1,#3,#4. Gap = G3 item #2 (context window fill + cache TTL).
- Backend-first task — UI consumes `/api/insights` field. Keeps frontend/backend in sync per CLAUDE.md.
- Depends on 001 (`cache_read`/`cache_creation` already wired in `tokensMap`).
- Reuses `src/shared/pricing.ts` from 001 for `contextWindow` + `cacheTtlMs` constants — single source.
- Active-agent-only output keeps payload small.
- Countdown computed client-side from `cacheExpiresMs` — no polling endpoint.

Verification note: planner task only writes spec markdown, no code to test. File written matches required format (Goal/Files/Acceptance/Notes), respects hard rules (TS strict, functional React, no ORM, no Claude co-author).
